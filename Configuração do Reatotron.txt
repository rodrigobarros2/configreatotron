0 fechar o bundler pra instalar certo

1 npm i reactotron-react-native      reactotron-redux      reactotron-redux-saga       caso irá utilizar

2 criar pasta config / ReactotronConfig.js

4 colocar dentro de ReactotronConfig.js

import Reactotron from 'reactotron-react-native';
import { AsyncStorage } from 'react-native';

if (__DEV__) {
	const tron = Reactotron.setAsyncStorageHandler(AsyncStorage)
		.configure()
		.useReactNative()
		.connect();

	tron.clear();

	console.tron = tron;
}


6 no index.js da aplicação import './config/ReactotronConfig'; na primeira linha
